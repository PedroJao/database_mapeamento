# Exemplo de arquivo de ambiente para o projeto de mapeamento
# Copie este arquivo para .env e preencha com seus valores locais.

# Configs do Banco de Dados (usadas pelo Docker Compose)
POSTGRES_DB=sas_foco_db
POSTGRES_USER=admin
POSTGRES_PASSWORD=sua_senha_secreta_aqui

# Configs do Django (usadas pelo settings.py)
# O DB_HOST deve ser o nome do serviço no docker-compose.yml
DB_HOST=db
DB_PORT=5432